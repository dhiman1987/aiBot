logging:
  level:
    com:
      dhiman:
        ai:
          bot: DEBUG
spring:
  datasource:
    url: jdbc:postgresql://${PGHOST}:${PGPORT}/${POSTGRES_DB}
    username: ${PGUSER}
    password: ${PGPASSWORD}

app:
  properties:
    setup-vector-store: ${SETUP_VECTOR_STORE}
    text-source: text-source.txt

